# Input/reference data (large, machine/run-specific — not versioned).
# Each person downloads/generates this locally via install/ and
# scripts/setup_real_smoke_test.sh.
data/
downloads/
test_data/

# Outputs generated by grn/ctx runs and their logs
outs/
logs/

# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments (this project uses conda outside the repo, just in case)
.venv/
venv/

# Editor / IDE / OS
.claude/
.vscode/
.idea/
.DS_Store
